import {StyleSheet, useColorScheme} from 'react-native';
import {Metrics} from '../metrics';
import {black, transparantBlack40, transparantWhite40, white} from '../color';


export const defaultStyles = StyleSheet.create({
  cardContainer: {
    borderRadius: 10,
    padding: 10,
    margin: 10
  },
  line: {
    height: 1,
    marginBottom: 5,
    marginTop: 2,
  },
});
